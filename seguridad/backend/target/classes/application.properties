# === CONEXI\u00d3N REAL A SUPABASE ===
spring.datasource.url=jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.nxwnkcuowajyiqxelnno
spring.datasource.password=Seguridad2025
spring.datasource.driver-class-name=org.postgresql.Driver

# === CONFIGURACI\u00d3N JPA ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === CONFIGURACI\u00d3N HIKARI (POOL) ===
spring.datasource.hikari.maximum-pool-size=1
spring.datasource.hikari.minimum-idle=1

# === CONFIGURACI\u00d3N DEL SERVIDOR ===
server.port=8081
spring.web.cors.allowed-origins=*
logging.level.org.springframework.web=DEBUG

# === LOGGING GENERAL DE LA APLICACI\u00d3N ===
# Nivel por defecto para toda la app
logging.level.root=INFO

# Logs de tu c\u00f3digo (paquete com.seguridadindustrial.backend)
logging.level.com.seguridadindustrial.backend=INFO

# Opcional: si quieres ver menos ruido de Spring en consola, puedes bajar esto a INFO
# logging.level.org.springframework.web=INFO

# === ACTUATOR: ENDPOINTS DE MONITOREO ===
management.endpoints.web.exposure.include=health,info,metrics,loggers
management.endpoint.health.show-details=always
# Usamos el mismo puerto del backend
management.server.port=8081
